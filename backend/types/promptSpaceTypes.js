const PROMPT_SPACE_TYPE = {
    Image: "image",
    Video: "video",
};

const PROMPT_SPACE_TYPE_VALUES = Object.values(PromptSpaceType);

module.exports = {
    PROMPT_SPACE_TYPE,
    PROMPT_SPACE_TYPE_VALUES,
};
